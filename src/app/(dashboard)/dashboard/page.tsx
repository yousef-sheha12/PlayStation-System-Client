import DashboardContent from '@/features/dashboard/components/dashboard-content';

export const metadata = {
  title: 'Dashboard | PlayStation System',
};

export default function DashboardPage() {
  return <DashboardContent />;
}
