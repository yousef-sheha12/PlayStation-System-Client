import DevicesContent from '@/features/devices/components/devices-content';

export const metadata = {
  title: 'Devices | PlayStation System',
};

export default function DevicesPage() {
  return <DevicesContent />;
}
