import ExpensesContent from '@/features/expenses/components/expenses-content';

export const metadata = { title: 'Expenses | PlayStation System' };

export default function ExpensesPage() {
  return <ExpensesContent />;
}
