import SettingsContent from '@/features/settings/components/settings-content';
export const metadata = { title: 'Settings | PlayStation System' };
export default function SettingsPage() { return <SettingsContent />; }
